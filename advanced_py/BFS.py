from collections import deque

# Definition for a binary tree node.
class TreeNode(object):
    def __init__(self, x):
        self.val = x
        self.left = None
        self.right = None
class BFS(object):
  def bfs(self, root):
    """
    input: TreeNode root
    return: Integer[]
    """
    d = deque()
    # queue = []
    # queue.append(1)
    # queue.pop()
    # queue.
    d.pop()
